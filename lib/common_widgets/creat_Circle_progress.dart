import 'package:flutter/material.dart';

class CreatCircleProgressIndicator extends StatelessWidget {


  @override
  Widget build(BuildContext context) {
    return new Center(
        child: Center(child: new CircularProgressIndicator()));
  }
}
